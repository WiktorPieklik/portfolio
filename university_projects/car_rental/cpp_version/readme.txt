C++ version of my objective language course project.
As a db it uses local MySql db.

Use Setup.msi to run app, but before make sure you have local db with following params: port=3307;username=root;password=root.
